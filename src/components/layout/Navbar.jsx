export default function Navbar() {
  /* TODO:
      Only show the home page when not authenticated
      Don't show all pages list to students?
       */

  return (
    <div>
      <h1>I am the Navbar!</h1>
    </div>
  );
}
